name: Sign with Sigstore
on: [push]
permissions:
  contents: write 
  id-token: write
jobs:
  sign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: sigstore/cosign-installer@main
      - name: Sign the file
        env:
          COSIGN_EXPERIMENTAL: 1
        run: |
          cosign sign-blob --output-signature sihol.ps1.sig sihol.ps1
      - name: Verify signature
        run: |
          cosign verify-blob --signature sihol.ps1.sig sihol.ps1
      - name: Commit signature  
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add sihol.ps1.sig
          git commit -m "Add signature for sihol.ps1" || exit 0
          git push